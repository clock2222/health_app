<div class="base-content">
  <%= render "hashtag_list" %>
  <h1 class="text-center">レシピの一覧</h1>
  <div class="d-flex justify-content-center">
    <%= paginate @posts %>
  </div>
  <div class="row">
    <%= render @posts %>
  </div>
  <div class="d-flex justify-content-center">
    <%= paginate @posts %>
  </div>
</div>
