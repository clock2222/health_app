<div class="base-content">
  <%= render "hashtag_list" %>
  <h1 class="text-center mt-5">レシピの一覧</h1>
  <div class="d-flex justify-content-center mt-5">
    <%= paginate @posts %>
  </div>
  <div class="row mt-3">
    <%= render @posts %>
  </div>
  <div class="d-flex justify-content-center mt-5">
    <%= paginate @posts %>
  </div>
</div>
